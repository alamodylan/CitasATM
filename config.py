# config.py

import os


class Config:

    # =====================================================
    # CONFIGURACIÓN GENERAL
    # =====================================================
    SECRET_KEY = os.environ.get(
        "SECRET_KEY",
        "atm_secret_key_2026"
    )

    # =====================================================
    # BASE DE DATOS
    # =====================================================
    DATABASE_URL = os.environ.get(
        "DATABASE_URL"
    )
    print(
        "DATABASE_URL EXISTS:",
        bool(DATABASE_URL)
    )
    # =====================================================
    # APP
    # =====================================================
    DEBUG = (
        os.environ.get(
            "DEBUG",
            "False"
        ).lower() == "true"
    )

    PORT = int(
        os.environ.get(
            "PORT",
            5000
        )
    )

    # =====================================================
    # ZONA HORARIA
    # =====================================================
    TIMEZONE = "America/Costa_Rica"

    # =====================================================
    # LÍMITE CITAS POR HORARIO
    # =====================================================
    SLOT_LIMIT = 5

    # =====================================================
    # ROLES
    # =====================================================
    ROLES = [
        "ADMIN",
        "PREDIO",
        "GUARDA"
    ]

    # =====================================================
    # ESTADOS DE CITAS
    # =====================================================
    ESTADOS_CITAS = [
        "Pendiente",
        "Vencida",
        "Completada"
    ]

    # =====================================================
    # NAVIERAS
    # =====================================================
    NAVIERAS = [
        "COSCO",
        "ONE",
        "OOCL",
        "VASI"
    ]

    # =====================================================
    # TIPOS OPERACIÓN
    # =====================================================
    TIPOS_OPERACION = [
        "Retira",
        "Entrega"
    ]

    # =====================================================
    # ESTADOS CONTENEDOR
    # =====================================================
    ESTADOS_CONTENEDOR = [
        "Cargado",
        "Vacio"
    ]